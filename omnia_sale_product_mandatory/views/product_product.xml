<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="product_product_mandatory_form" model="ir.ui.view">
            <field name="name">product.product.mandatory.form</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_normal_form_view"/>
            <field name="arch" type="xml">
                <field name="company_id" position="before">
                    <field name="needed_children_product_ids" widget="many2many_tags"/>
                </field>
            </field>
        </record>

</odoo>